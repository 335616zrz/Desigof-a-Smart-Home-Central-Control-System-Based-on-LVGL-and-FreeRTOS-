<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iot.mapper.TelemetryMapper">

    <resultMap id="TelemetryResultMap" type="iot.domain.TelemetryRecord">
        <id property="id" column="id" />
        <result property="deviceId" column="device_id" />
        <result property="payload" column="payload" />
        <result property="reportedAt" column="reported_at" />
    </resultMap>

    <insert id="insert" parameterType="iot.domain.TelemetryRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO device_telemetry (device_id, payload, reported_at)
        VALUES (#{deviceId}, #{payload}, #{reportedAt})
    </insert>

    <select id="findLatestByDevice" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        WHERE device_id = #{deviceId}
        ORDER BY reported_at DESC
        LIMIT #{limit}
    </select>

    <select id="findByDeviceAndRange" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        WHERE device_id = #{deviceId}
          AND reported_at BETWEEN #{start} AND #{end}
        ORDER BY reported_at DESC
        LIMIT #{limit}
    </select>

    <select id="findByDevicePaged" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        WHERE device_id = #{deviceId}
        <if test="start != null and end != null">
            AND reported_at BETWEEN #{start} AND #{end}
        </if>
        ORDER BY reported_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByRangePaged" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        <if test="start != null and end != null">
            WHERE reported_at BETWEEN #{start} AND #{end}
        </if>
        ORDER BY reported_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByDeviceAndRange" resultType="long">
        SELECT COUNT(*)
        FROM device_telemetry
        WHERE device_id = #{deviceId}
        <if test="start != null and end != null">
            AND reported_at BETWEEN #{start} AND #{end}
        </if>
    </select>

    <select id="countByRange" resultType="long">
        SELECT COUNT(*)
        FROM device_telemetry
        <if test="start != null and end != null">
            WHERE reported_at BETWEEN #{start} AND #{end}
        </if>
    </select>

    <select id="findLatestWithin" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        WHERE device_id = #{deviceId}
          AND reported_at >= #{since}
        ORDER BY reported_at DESC
        LIMIT 1
    </select>

    <select id="findAllLatest" resultMap="TelemetryResultMap">
        SELECT id, device_id, payload, reported_at
        FROM device_telemetry
        ORDER BY reported_at DESC
        LIMIT #{limit}
    </select>
</mapper>
