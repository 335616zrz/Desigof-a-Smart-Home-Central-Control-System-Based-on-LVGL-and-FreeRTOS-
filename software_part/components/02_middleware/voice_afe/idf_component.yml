dependencies:
  espressif/gmf_ai_audio: ">=0.6.1"
  espressif/esp-sr: ">=2.1.1"

description: "Voice AFE wrapper that bridges INMP441 PCM to WakeNet/MultiNet (optional) and remote ASR streaming"
version: "0.1.0"
